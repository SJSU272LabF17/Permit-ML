// Logic to govern the user page

// Unique namespace
var DTL = DTL || {};

DTL.dataObj = {};

// Get the data from the server on load, and store it
DTL.setDataObj = function(data) {
  this.dataObj = data;
};

DTL.showProfile = function() {};

// Log out user
DTL.logoutUser = function() {};

// JQuery functions

jQuery(document).ready(function($) {

});








